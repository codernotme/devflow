import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bgPrimary = Color(0xFF0F1117);
  static const bgSurface = Color(0xFF1A1D27);
  static const bgElevated = Color(0xFF22263A);
  static const bgBorder = Color(0xFF2E3347);

  // Text
  static const textPrimary = Color(0xFFE5E5E5);
  static const textSecondary = Color(0xFFA0A0A0);
  static const textMuted = Color(0xFF555C6E);
  static const textInverse = Color(0xFF0F1117);

  // Accents
  static const accentBlue = Color(0xFF4A9EED);
  static const accentPurple = Color(0xFF8B5CF6);
  static const accentGreen = Color(0xFF22C55E);
  static const accentAmber = Color(0xFFF59E0B);
  static const accentRed = Color(0xFFEF4444);
  static const accentCyan = Color(0xFF06B6D4);
}
